# TODO — известные проблемы данных (не layout-кода)

## F107 / F109 — дублирующаяся family-запись (P310 ↔ P174)

`tree-fallback.json`: `F107` и `F109` — две отдельные family-записи для
**одной и той же** супружеской пары (husband=P310, wife=P174 в обеих):

```
F107 = { husband: "P310", wife: "P174", children: [] }
F109 = { husband: "P310", wife: "P174", children: ["P101"] }
```

Это не повторный брак и не полигамия — husband/wife совпадают полностью.
Похоже на случайное дублирование записи при ручном/автоматическом
редактировании дерева (одна пустая, другая с ребёнком).

**Нужно** (не задача сессии layout-кода, а работа с базой/админ-панелью):
- объединить в одну family-запись — сохранить `P101` как ребёнка,
  удалить дублирующую (пустую) запись;
- сделать это через `admin.html` → «Загрузить дерево» после ручной правки
  JSON, либо через прямой `PATCH`/`DELETE /api/family/:id` (см.
  `src/index.js`), а не редактированием layout-алгоритма — сам алгоритм
  раскладки (`tree-print-layout3.js`) корректно отражает то, что задано
  в данных, включая этот дубль (отсюда видимый в тесте разрыв `gap=518`,
  посчитанный дважды — по одному разу на каждую из двух family-записей).

Обнаружено в ходе диагностики разрывов пар в `placeFamiliesInRow()`
(см. комментарий в шапке `tree-print-layout3.js`).

## 29 наложений карточек в placeFamiliesInRow() (ветка onePlaced) — known limitation

**Статус: known limitation, не критично для текущего использования.**

Визуальный аудит `layoutAllComponents()` на всех 239 персонах / 41 компоненте
выявил 29 случаев, где карточка из ветки onePlaced (супруг(а), чья позиция
вычисляется от уже размещённого «владельца») пересекается (`|x1-x2| < CW`
на одном `y`) с уже размещённой extern-позицией — соседом из children-block
предыдущего row или другой семьи на том же row.

Три независимые попытки фикса (глобальный row-floor через `occupiedRightEdge`,
точечная pre-sweep проверка коллизии, anchor-check внутри существующего
sequential prevRight-прохода) — каждая полностью устраняла все 29
пересечений, но при этом создавала каскадную регрессию: 24+ новые пары
с большим разрывом (`gap>300`) там, где раньше стояла корректная пара
вплотную (`gap=12`, `COUPLE_GAP`). Причина — структурная: `prevRight` в
едином проходе `placeFamiliesInRow` накапливается **последовательно через
весь row**, и любой локальный сдвиг одной задачи вправо (чтобы закрыть её
конкретное пересечение) автоматически сдвигает `prevRight` для **всех**
последующих задач этого же вызова, даже если у них самих не было
пересечения ни с чем. На многолюдных row (например, 130-персонная
компонента) это превращает один точечный фикс в цепную реакцию.

**Требуется** для полноценного исправления: алгоритм компакции по
кластерам (union-find/graph-based — сначала выявить независимые
«островки» задач на row, устранять пересечения ВНУТРИ каждого острова
отдельно, не давая сдвигу одного острова влиять на другие), а не точечный
патч поверх текущего однонаправленного sweep. Это отдельная, более крупная
задача по перепроектированию `placeFamiliesInRow`/`placeChildrenOfFamiliesInRow`,
не входит в scope точечных фиксов.

См. комментарий `KNOWN LIMITATION` в шапке `placeFamiliesInRow()` в
`tree-print-layout3.js`.

## Аудит живых данных 23.09.2026 (backup `mostovoy-tree-backup-2026-09-23.json`, 257 персон / 122 семьи)

Ниже — три находки чисто в данных (не в коде layout/печати), обнаруженные
при проверке причин отсутствующих линий-связей на печати. Ничего не
исправлено — это фиксация для отдельного решения.

### 1. Gen-несовпадения у супругов — 27 из 83 пар

Для каждой семьи, где заданы оба супруга, `IDX.nodes[husband].gen` должен
совпадать с `IDX.nodes[wife].gen` — иначе супруги физически попадают в
разные ряды на печати, и линия-связь между ними не рисуется (see:
scale-guard в `tree-print.js`, Этап 1). Проверка по живому `tree_data`
(не по устаревшему `tree-fallback.json` — тот давал только 9/69):

**⚠ ПРИОРИТЕТ: F1 — Яков Мостовой [P1] (gen=2) / Сицилия Мостовая [P2]
(gen=3), Δ=−1.** Это родители фокальной персоны (Мирослава Мостового) —
главная пара всего дерева, у неё тоже не рисуется линия-сердце на печати.

Полный список (`семья: муж (gen) <-> жена (gen), Δ`):

```
F1:   Яков Мостовой (2) [P1]                    <-> Сицилия Мостовая (3) [P2]                       Δ=-1   ⚠ ПРИОРИТЕТ (родители фокальной персоны)
F3:   Лейцис Александр Львович (1) [P5]         <-> Маша Пейсахович (2) [P24]                       Δ=-1
F5:   Евгений Гинзбург (3) [P11]                <-> Людмила Гинзбург (4) [P6]                       Δ=-1
F8:   Эдик Витинбергский (2) [P26]              <-> Ира Витинбергская (3) [P25]                     Δ=-1
F11:  Боря (3) [P35]                             <-> Вика Кравцова (4) [P27]                         Δ=-1
F17:  Левик Ицик Пейсахович (1) [P42]           <-> Зинаида Исааковна Розенберг (0) [P218]          Δ=1    (см. п.2 — дубль F60)
F19:  Леонид Вайнтроб (3) [P48]                 <-> Светлана Игнатенко (2) [P50]                    Δ=1
F23:  Фима Щиглик (1) [P61]                     <-> Соня Журахинская (2) [P58]                      Δ=-1
F24:  Саша Щиглик (3) [P63]                     <-> Лариса Сегал (2) [P64]                          Δ=1
F25:  Алик Щиглик (3) [P62]                     <-> Лена Голдберг (2) [P65]                         Δ=1
F26:  Саша Щиглик (3) [P63]                     <-> Юля (2) [P67]                                   Δ=1
F35:  Митя Берман (1) [P121]                    <-> Беба Берман (ур. Кандыба) (2) [P91]              Δ=-1
F50:  Даня Лейцис (2) [P32]                     <-> Феня Мойсеевна Мининская (1) [P200]              Δ=1
F58:  Станислав Семеновский (2) [P215]          <-> Оля Вайнтроб (3) [P49]                           Δ=-1
F60:  Левик Ицик Пейсахович (1) [P42]           <-> Зинаида Исааковна Розенберг (0) [P218]          Δ=1    (см. п.2 — дубль F17)
F64:  Владислав Игдал (3) [P225]                <-> Евгения Игдал (4) [P223]                        Δ=-1
F65:  Анатолий Ямпольский (2) [P228]            <-> Элеонора (Элла) Журахинская (3) [P141]          Δ=-1
F66:  Михаэль Ямпольский (4) [P229]             <-> Виктория Ямпольская (3) [P230]                  Δ=1
F79:  Туля Пейсахович (1) [P259]                <-> Рахиля (0) [P291]                                Δ=1    (см. п.2 — дубль F95)
F95:  Туля Пейсахович (1) [P259]                <-> Рахиля (0) [P291]                                Δ=1    (см. п.2 — дубль F79)
F96:  Пейсахович Алексей-Иосиф Анатольевич (2) [P288] <-> Пейсахович Шуламис-Света Мееровна (0) [P293]  Δ=2
F104: Лев Зильберман (gen=отсутствует) [P307]   <-> Хана Мостовая (1) [P80]                          Δ=?    (см. п.3 — gen отсутствует)
F107: Давид Зицер (0) [P310]                    <-> Хана Зицер (gen=отсутствует) [P174]              Δ=?    (см. п.2 и п.3 — дубль F109 + gen отсутствует)
F109: Давид Зицер (0) [P310]                    <-> Хана Зицер (gen=отсутствует) [P174]              Δ=?    (см. п.2 и п.3 — дубль F107 + gen отсутствует)
F110: Игорь Берман (3) [P313]                   <-> Алла Берман (2) [P315]                           Δ=1
F111: Янэк Берман (3) [P314]                    <-> Рая (2) [P316]                                   Δ=1
F123: Толик (3) [P349]                          <-> Зина Семеновская (4) [P216]                      Δ=-1
```

### 2. Дублирующиеся family-записи одной и той же пары под разными id — **тот же баг-класс, не единичный случай**

Ранее в этом файле (см. раздел выше) был задокументирован единичный
случай F107/F109. Аудит живых данных 23.09.2026 показал, что это
**системная, повторяющаяся проблема**, а не разовая накладка:

- `F107` / `F109` — Давид Зицер [P310] × Хана Зицер [P174] (уже известно)
- `F17` / `F60` — Левик Ицик Пейсахович [P42] × Зинаида Исааковна Розенберг [P218] (новое)
- `F79` / `F95` — Туля Пейсахович [P259] × Рахиля [P291] (новое)

Во всех трёх случаях — одна и та же пара супругов существует как две
отдельные family-записи в `families`. Раз найдено уже 3 независимых
случая — стоит рассматривать это не как точечную правку трёх записей, а
как признак систематической причины дублирования (при повторном
"привязать существующего"/"добавить семью" через админку/API), которую
стоит поискать и устранить в `POST /api/family` / логике привязки, а не
только вручную схлопнуть уже возникшие дубли.

### 3. Полностью отсутствует поле `gen` (не 0, а `undefined`)

- **P307** — Лев Зильберман
- **P174** — Хана Зицер

Это грубее, чем несовпадение gen из п.1 — тут значения нет вовсе, что
влияет и на печать (см. п.1), и потенциально на другую логику, читающую
`node.gen` (например `layoutAllComponents`/`assignRows`, которые молча
пропускают персону без gen — см. `assignRows()` в
`tree-print-layout2.js`/`tree-print-layout3.js`).

